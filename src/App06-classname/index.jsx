import React from "react";
import ReactDOM from "react-dom";

const name = "Two Sigma";

ReactDOM.render(
  <h1 className="heading">
    <div>Hello, {name}!</div>
  </h1>,
  document.getElementById("root"),
);
